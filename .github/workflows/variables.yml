name: variables

on:
  workflow_run:
    workflows:
      - Lint
    types:
      - completed

jobs:
  variables:
    runs-on: ubuntu-latest
    steps:
      - name: Dump GitHub context
        env:
          GITHUB_CONTEXT: ${{ toJson(github) }}
        run: |
          echo "$GITHUB_CONTEXT"
